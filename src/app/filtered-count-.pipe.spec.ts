import { FilteredCountPipe } from './filtered-count-.pipe';

describe('FilteredCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredCountPipe();
    expect(pipe).toBeTruthy();
  });
});
